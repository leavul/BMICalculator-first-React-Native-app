import { theme } from "../../constants/theme";

const style = {
    fontFamily: 'Ubuntu Regular',
    backgroundColor: theme.colors.measurementInputBackground,
    borderRadius: theme.borderRadius.small,
    padding: theme.paddingAndMargin.medium,
    marginTop: theme.paddingAndMargin.small,
    marginBottom: theme.paddingAndMargin.xlarge,
};

export default style;